{{!< default}}

<main class="container">

    {{#tag}}
    <section class="mt-8 mb-6">
        <h1 class="text-3xl">
            {{name}}
        </h1>
        {{#if description}}
            <div class="mt-2">{{description}}</div>
        {{/if}}
    </section>
    {{/tag}}

    <div class="grid grid-flow-row divide-y">
        {{#foreach posts}}
            {{> "loop"}}
        {{/foreach}}
    </div>

    {{pagination}}

</main>