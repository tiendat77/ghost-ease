routes:

collections:
  /releases/:
    permalink: /releases/{slug}/
    template: tag-releases
    filter: primary_tag:releases
  /:
    permalink: /{slug}/
    template: index

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/
